<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>Edit a Product</h1>
    <form action="/products/<%=editProduct._id%>?_method=PUT" method="POST">
        <label for="name">Product Name</label>
        <input type="text" name="name" id="name" placeholder="product name" value="<%=editProduct.name%>  ">
        <label for="price">Price (Unit)</label>
        <input type="number" name="price" id="price" step="any" value="<%=editProduct.price %>">
        <label for="category">Select Category</label>
        <select name="category" id="category">
            <% for( let index=0; index < categories.length; index++ ) { %>
                <option value="<%=categories[index]%>" <%=editProduct.category===categories[index] ? "selected" : "" %>>
                    <%=categories[index]%>
                </option>
                <% } %>
        </select>
        <button type="submit">Submit</button>
    </form>


    <a href="/products/<%=editProduct._id%> ">Back</a>


</body>

</html>